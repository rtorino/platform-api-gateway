'use strict';

module.exports = {
	'handleCreateResponse' : function handleCreateResponse ( response, done ) {
		return;
	},

	'handleReadResponse' : function handleReadResponse ( response, done ) {
		return;
	},

	'handleUpdateResponse' : function handleUpdateResponse ( response, done ) {
		return;
	},

	'handleDeleteResponse' : function handleDeleteResponse ( response, done ) {
		return;
	}
};
